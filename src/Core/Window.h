//  ██████╗  █████╗ ███╗   ███╗███╗   ███╗ █████╗  
//  ██╔════╝ ██╔══██╗████╗ ████║████╗ ████║██╔══██╗
//  ██║  ███╗███████║██╔████╔██║██╔████╔██║███████║
//  ██║   ██║██╔══██║██║╚██╔╝██║██║╚██╔╝██║██╔══██║
//  ╚██████╔╝██║  ██║██║ ╚═╝ ██║██║ ╚═╝ ██║██║  ██║
//   ╚═════╝ ╚═╝  ╚═╝╚═╝     ╚═╝╚═╝     ╚═╝╚═╝  ╚═╝
//
// ================================================================================
// Window.h
// Отвечает за создание системного окна, обработку сообщений ввода (WinAPI)
// и управление жизненным циклом окна.
// ================================================================================

#pragma once
#include "Prerequisites.h"
#include "Logger.h"

class Window {
public:
    Window();
    ~Window();

    // Создает окно заданного разрешения и с заголовком.
    bool Initialize(int width, int height, const std::string& title);

    // Обрабатывает очередь сообщений. Возвращает false, если получено сообщение о закрытии.
    bool ProcessMessages();

    // Получает дескриптор окна (нужен для DirectX).
    HWND GetHWND() const { return m_hwnd; }

    // Изменяет заголовок окна (например, для вывода FPS).
    void SetTitle(const std::string& title);

private:
    HWND m_hwnd = nullptr; // Дескриптор окна

    // Статическая процедура обработки сообщений Windows
    static LRESULT CALLBACK WindowProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam);
};